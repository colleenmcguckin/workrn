Sign In
.row
  = simple_form_for session_path(:user) do |f|
    =f.input :email
    =f.input :password
    = f.input :remember_me, as: :boolean, inline_label: 'Yes, remember me'
    =link_to 'Sign In', user_session_path
    = link_to "Forgot your password?", new_password_path(:user)
